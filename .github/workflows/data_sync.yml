name: Daily Data Sync

on:
  schedule:
    - cron: '0 0 * * *' # Run at midnight UTC daily
  workflow_dispatch: # Allow manual trigger

jobs:
  sync_imd_data:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Needed to push commits
      
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install Dependencies
        run: pip install -r requirements.txt

      - name: Sync Live IMD Data
        run: python scripts/sync_live_imd.py

      - name: Configure MOSDAC API Secrets
        run: |
          cat <<EOF > config.json
          {
            "mosdac_username": "${{ secrets.MOSDAC_USERNAME }}",
            "mosdac_password": "${{ secrets.MOSDAC_PASSWORD }}"
          }
          EOF

      - name: Sync and Decode Live MOSDAC HDF5 Data
        run: python scripts/download_insat_live.py

      - name: Commit and Push Changes
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add data/processed/*.nc
          
          # Only commit if there are changes
          if ! git diff --staged --quiet; then
            git commit -m "chore(data): auto-sync live IMD datasets"
            git push
          else
            echo "No new data to commit."
          fi
